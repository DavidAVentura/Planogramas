$kind: http-request
url: "{{HOST}}/api/v1/versiones/{{versionIdPublicada}}/guardar"
method: PATCH
scripts:
  - type: afterResponse
    code: pm.test('Status 200', () => pm.response.to.have.status(200));
    language: text/javascript
order: 12000
